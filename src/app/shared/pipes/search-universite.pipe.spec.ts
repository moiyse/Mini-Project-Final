import { SearchUniversitePipe } from './search-universite.pipe';

describe('SearchUniversitePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchUniversitePipe();
    expect(pipe).toBeTruthy();
  });
});
